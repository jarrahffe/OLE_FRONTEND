import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      // MUI: blue 800
      main: "#1565c0",
    },
    warning: {
      main: "#c2185b"
    }
  }
});

export default Theme