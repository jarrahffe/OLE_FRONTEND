import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      // MUI: blue 800
      main: "#1565c0"
    },
    secondary: {
      // MUI: blue 50
      main: "#e3f2fd"
    }
  }
});

export default Theme